[package]
name = "vram-map"
version = "0.1.0"
edition = "2021"
description = "GPU memory visualizer TUI for AMD Radeon 890M (UMA iGPU)"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
anyhow = "1"

[profile.release]
opt-level = 2
strip = true
